# Optional desktop icons for electron-builder (itch / Steam)
# Drop a square PNG here named icon.png (≥256×256, ideally 512), then:
#   npm run desktop:dist
# Windows also accepts icon.ico; macOS icon.icns — builder can derive from PNG.
